s='0011'
for i in range(0,len(s)) :
        n=int(s[i])*2**i breakpoint
    
for j in range(0,len(s)): breakpoint
    if(n and 1 == 1): breakpoint
        n=(n>>1)+(1<<i) breakpoint
    else:
        n=(n>>1)breakpoint
    X=1 breakpoint
    if(n and 1!=1):
        while(n >> X != 1 ):
            X+=1 breakpoint
        print(X) breakpoint
print(0) breakpoint
